<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="apppasteleriav04.Views.Sync.SyncSettingsPage"
             Title="Configuración de Sync">
    
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="16">
            
            <Label Text="Configuración" FontSize="24" FontAttributes="Bold"/>
            
            <!-- Auto Sync -->
            <Frame BackgroundColor="#F5F5F5" CornerRadius="10" Padding="16">
                <Grid ColumnDefinitions="*,Auto">
                    <VerticalStackLayout Grid.Column="0">
                        <Label Text="Sincronización Automática" FontSize="16" FontAttributes="Bold"/>
                        <Label Text="Sincronizar datos automáticamente" FontSize="12" TextColor="#666"/>
                    </VerticalStackLayout>
                    <Switch x:Name="AutoSyncSwitch" 
                            Grid.Column="1" 
                            Toggled="OnAutoSyncToggled"
                            OnColor="#6C5CE7"/>
                </Grid>
            </Frame>
            
            <!-- Sync Interval -->
            <Frame BackgroundColor="#F5F5F5" CornerRadius="10" Padding="16">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Intervalo de Sincronización" FontSize="16" FontAttributes="Bold"/>
                    <Picker x:Name="IntervalPicker" 
                            Title="Seleccionar intervalo"
                            SelectedIndexChanged="OnIntervalChanged">
                        <Picker.Items>
                            <x:String>Cada 5 minutos</x:String>
                            <x:String>Cada 15 minutos</x:String>
                            <x:String>Cada 30 minutos</x:String>
                            <x:String>Cada hora</x:String>
                        </Picker.Items>
                    </Picker>
                </VerticalStackLayout>
            </Frame>
            
            <!-- WiFi Only -->
            <Frame BackgroundColor="#F5F5F5" CornerRadius="10" Padding="16">
                <Grid ColumnDefinitions="*,Auto">
                    <VerticalStackLayout Grid.Column="0">
                        <Label Text="Solo WiFi" FontSize="16" FontAttributes="Bold"/>
                        <Label Text="Sincronizar solo con conexión WiFi" FontSize="12" TextColor="#666"/>
                    </VerticalStackLayout>
                    <Switch Grid.Column="1" OnColor="#6C5CE7"/>
                </Grid>
            </Frame>
            
            <!-- Actions -->
            <Label Text="Acciones" FontSize="18" FontAttributes="Bold" Margin="0,16,0,0"/>
            
            <Button Text="🗑️ Limpiar Caché Local"
                    Clicked="OnClearCacheClicked"
                    BackgroundColor="#E74C3C"
                    TextColor="White"
                    CornerRadius="8"/>
            
            <Button Text="🔄 Forzar Sincronización Completa"
                    Clicked="OnForceFullSyncClicked"
                    BackgroundColor="#3498DB"
                    TextColor="White"
                    CornerRadius="8"/>
                    
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>