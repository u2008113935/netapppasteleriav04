<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns: x="http://schemas.microsoft.com/winfx/2009/xaml"
             x: Class="apppasteleriav04.Views.Shared.SplashPage"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="#1C1C1E">

    <Grid>
        <!-- Gradiente de fondo -->
        <BoxView>
            <BoxView.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#FF6B9D" Offset="0.0" />
                    <GradientStop Color="#FFA8C5" Offset="1.0" />
                </LinearGradientBrush>
                </BoxView. Background>
        </BoxView>

        <!-- Contenido -->
        <VerticalStackLayout VerticalOptions="Center" 
                            HorizontalOptions="Center"
                            Spacing="30">

            <!-- Logo (reemplazar con tu imagen) -->
            <Label Text="🍰" 
                   FontSize="120"
                   HorizontalOptions="Center"/>

            <Label Text="Pastelería Delicia" 
                   FontSize="32"
                   FontAttributes="Bold"
                   TextColor="White"
                   HorizontalOptions="Center"/>

            <Label Text="Dulzura en cada bocado" 
                   FontSize="16"
                   TextColor="White"
                   Opacity="0.9"
                   HorizontalOptions="Center"/>

            <!-- Loading -->
            <ActivityIndicator IsRunning="True" 
                              Color="White"
                              HeightRequest="40"
                              Margin="0,30,0,0"/>

        </VerticalStackLayout>

        <!-- Versión -->
        <Label Text="v3.0.0" 
               FontSize="12"
               TextColor="White"
               Opacity="0.7"
               VerticalOptions="End"
               HorizontalOptions="Center"
               Margin="0,0,0,30"/>
    </Grid>

</ContentPage>